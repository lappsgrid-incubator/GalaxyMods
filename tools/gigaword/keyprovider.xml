<tool id='gigaword.key.provider' name='OAuth Key Provider' version='1.0.0'>
    <description>Provides an OAuth token to tools that require one.</description>
    <command interpreter='lsd'>keyprovider.lsd $token $output</command>
    <inputs>
        <param name="token" type="text" label="OAuth Token"/>
    </inputs>
    <outputs>
        <data name='output' format='oauth'/>
    </outputs>
    <help><![CDATA[

Provides an OAuth2 token to Gigaword services.

.. class:: infomark

To obtain documents from Gigaword you **must**:

1. Be an LDC licensee.
2. `Obtain an OAuth Token <https://grid.anc.org:9443/ldc-authorize/>`_

To obtain an OAuth token visit the link above and log in with your LDC id and password. Then paste
the OAuth token into the above field and click `execute`.  This will add an item to your
history that can be used to supply the OAuth token to other Gigaword services that 
require it.

]]>
    </help>
</tool>