<tool id="deiis.evaluator" name="Evaluator" version="1.0.0">
    <description>Calculates precision, recall, and F1 score.</description>
    <command>lsd $__tool_directory__/invoke.lsd Evaluator $input $output</command>
    <inputs>
        <param name="input" type="data" format="txt"/>
    </inputs>
    <outputs>
        <data name="output" format="txt" label="Evaluated"/>
    </outputs>
    <help><![CDATA[
Calculates the precision, recall and the F1 score for the ranked candidate answers.  The top *N* answers are considered correct.

]]>
    </help>
</tool>
